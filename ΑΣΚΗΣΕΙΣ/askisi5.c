#include <stdio.h>

int main()
{
    int a,b,c;
    printf("Give first number:");
    scanf("%d",&a);
    printf("Give second number:");
    scanf("%d",&b);
    printf("Give third number:");
    scanf("%d",&c);
    int mul=a*b*c;
    printf("Multiplie result:%d",mul);
    return 0;
}